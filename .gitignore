.idea
.vscode
upload
*.exe
*.db
build
*.db-journal
logs
data
/web/node_modules
cmd.md
.env
.env.*
!.env.example
/one-api
temp
.DS_Store
Thumbs.db

# Node / npm (commitlint at repo root)
node_modules/
*.log
npm-debug.log*

# Mega-Linter
megalinter-reports/

# Go test coverage
coverage/
coverage.txt
*.out

# Local docker compose overrides. Personal tweaks belong here — docker compose
# loads docker-compose.override.yml automatically alongside docker-compose.yml.
docker-compose.override.yml
docker-compose.override.yaml

# Go vendored deps (not used currently)
vendor/

# Local dev artifacts — see CLAUDE.md §7 "Never commit local dev artifacts"
# Playwright MCP output (page snapshots + console logs from smoke tests)
.playwright-mcp/
# Chrome DevTools MCP browser profile cache
.cache/chrome-devtools-mcp/
# Verification screenshots dropped at repo root by Playwright / Chrome DevTools.
# Legitimate product assets belong under web/wavy/public/ or web/web_reference/
# — those are NOT matched by these root-only patterns.
/*.png
/*.jpg
/*.jpeg
/*.webp